{
  "name": "workshops",
  "version": "0.1.1",
  "description": "workshops",
  "author": "people and code",
  "type": "module",
  "scripts": {
    "lint": "eslint . && prettier --check . && stylelint '**/*.{css,scss}'",
    "static": "cp src/robots.txt dist/robots.txt",
    "start": "parcel && npm run static",
    "start:no-cache": "parcel --no-cache && npm run static",
    "build": "parcel build --dist-dir dist"
  },
  "source": [
    "./src/index.html",
    "./src/workshops/workshop-template.html"
  ],
  "devDependencies": {
    "eslint": "latest",
    "prettier": "latest",
    "stylelint": "latest",
    "parcel": "latest",
    "stylelint-config-standard": "latest"
  }
}
